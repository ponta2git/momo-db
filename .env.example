# Unpooled connection URL for drizzle-kit (migration/generate/check)
# Use a direct connection (not via PgBouncer) to avoid issues with CREATE INDEX CONCURRENTLY etc.
DIRECT_URL=postgres://user:password@host:5432/dbname
